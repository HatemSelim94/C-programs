### Common maximum number between two arrays:
* Finds the common maximum number between two arrays.
* Sorts one array then find the match by comparing both but selects the sorted array values in descending order.